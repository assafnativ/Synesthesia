<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_game"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.synesthesia.GameActivity">

    <Button
        android:id="@+id/lessButton"
        android:text="Less"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:onClick="onLessButtonClick" />

    <Button
        android:id="@+id/moreButton"
        android:text="More"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lessButton"
        android:layout_alignParentEnd="true"
        android:onClick="onMoreButtonClick" />

    <TextView
        android:id="@+id/counterTextBox"
        android:text="0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/moreButton"
        android:layout_above="@+id/moreButton"
        android:layout_alignParentEnd="true"
        android:textSize="24sp"
        android:textAlignment="center" />
</RelativeLayout>
