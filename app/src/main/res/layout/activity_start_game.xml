<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start_game"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.synesthesia.StartGame">

    <Button
        android:text="Phi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/piButton"
        android:layout_alignEnd="@+id/piButton"
        android:id="@+id/phiButton"
        android:onClick="onPhiButtonClick"
        android:textAppearance="@style/TextAppearance.AppCompat" />

    <Button
        android:text="Pi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/piButton"
        android:onClick="onPiButtonClick"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textAppearance="@style/TextAppearance.AppCompat" />

    <Button
        android:text="Custom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/phiButton"
        android:layout_alignStart="@+id/phiButton"
        android:id="@+id/customNumberButton"
        android:textAppearance="@style/TextAppearance.AppCompat"
        android:onClick="onCustomNumberButtonClick" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="10"
        android:layout_below="@+id/customNumberButton"
        android:layout_centerHorizontal="true"
        android:id="@+id/customNumberText"
        android:text="1234567890" />

</RelativeLayout>
